{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**"]
		},
		"check-types": {},
		"dev": {
			"cache": false
		},
		"format": {},
		"lint": {},
		"test": {
			"dependsOn": ["build"]
		}
	}
}
